# Backend requirements for Strang v3.0
# Groq (FREE) + HeyGen Avatar Video Generation

# Web framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
websockets==12.0  # WebSocket support for real-time progress

# AI Services
groq>=0.11.0       # Groq API for FREE and FAST script generation
httpx>=0.28.1      # HTTP client for async requests (HeyGen API)

# Utilities
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-dotenv==1.0.0
aiofiles==23.2.1

# Optional (for production)
redis==5.0.1       # For job queue persistence
